# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Android port of the [Orthodox Calendar iOS app](https://github.com/Ludikure/OrthodoxCalendar). Native Kotlin + Jetpack Compose. Orthodox Church calendar with daily saints, scripture readings, fasting guidance, and saint biographies. Supports Serbian, Russian, and English calendars. A bundled window of years (assets) works fully offline; years outside it (2024–2099) download once from the Cloudflare Worker's v2 archive (`/api/v2/{locale}/{year}`) and are cached permanently under `filesDir/calendar_cache` (excluded from backup). `dataRevision` from `/api/config` invalidates that cache after the archive is regenerated.

## Build

```bash
# Build debug APK
./gradlew assembleDebug

# Install on connected device/emulator
./gradlew installDebug

# APK location
app/build/outputs/apk/debug/app-debug.apk
```

Requires `local.properties` with `sdk.dir=/path/to/Android/sdk`.

## Architecture

```
app/src/main/java/com/orthodox/calendar/
├── MainActivity.kt                    # Single Activity, Compose entry, NavGraph
├── OrthodoxCalendarApp.kt             # Application class
├── data/
│   ├── model/                         # Data classes (@Serializable)
│   │   ├── CalendarDay.kt             # Core model with computed properties
│   │   ├── CalendarFile.kt            # Top-level JSON wrapper
│   │   ├── Feast.kt                   # Saint/feast entry (moveable flag, description)
│   │   ├── FastingInfo.kt, ScriptureReading.kt, SaintBio.kt, Reflection.kt
│   │   ├── AppLanguage.kt             # SR, RU, EN, EN_NC enum
│   │   ├── AppTheme.kt                # SYSTEM, LIGHT, DARK enum
│   │   └── LocalizationBundle.kt      # UILabels, ExtraFeast
│   ├── repository/CalendarRepository.kt  # assets → disk cache → v2 archive download
│   ├── network/ApiClient.kt           # Minimal HTTP GET helper (HttpURLConnection)
│   ├── localization/LocalizationManager.kt  # Loads locale bundles, caches
│   └── preferences/AppPreferences.kt  # DataStore for language/theme
├── ui/
│   ├── theme/
│   │   ├── AppColors.kt               # Adaptive colors via LocalIsDarkTheme
│   │   ├── Theme.kt                   # OrthodoxCalendarTheme, LocalIsDarkTheme provider
│   │   └── Type.kt                    # Serif typography
│   ├── navigation/
│   │   ├── Routes.kt                  # Sealed route classes
│   │   └── NavGraph.kt                # NavHost with all destinations
│   ├── viewmodel/CalendarViewModel.kt  # StateFlow + CalendarUiState
│   ├── screens/CalendarTabScreen.kt    # Main screen (header + month bar + list/grid)
│   ├── components/                     # Shared composables
│   │   ├── MonthHeaderBar.kt          # Month nav, list/grid toggle, date picker trigger
│   │   ├── CalendarTitle.kt           # App title header
│   │   ├── MonthListScreen.kt         # LazyColumn day list
│   │   └── DayRowView.kt             # Single day row
│   ├── screen/
│   │   ├── grid/                      # CalendarGridScreen, GridDayCell, SelectedDayCard, FastingLegend
│   │   ├── detail/                    # DayDetailScreen, SaintCard, ReadingCard
│   │   ├── search/SaintSearchScreen.kt
│   │   ├── settings/SettingsScreen.kt, LanguagePicker.kt
│   │   ├── about/AboutScreen.kt
│   │   ├── splash/SplashScreen.kt
│   │   ├── datepicker/DatePickerSheet.kt
│   │   └── reminder/AddReminderScreen.kt
│   └── util/Haptics.kt                # Haptic feedback helper
```

## Data Files

All in `app/src/main/assets/localization/`:
- `sr.json`, `ru.json`, `en.json` — UI strings
- `calendar_{sr,ru,en,en_nc}_{year}.json` — bundled-window year files (deduplicated)
- `texts_{sr,ru,en}.json` — text pools (bios + reading text, content-hash keyed). `en_nc` resolves against `texts_en.json` (`CalendarRepository.poolName`): the two were byte-identical, so a separate `texts_en_nc.json` was 3 MB of duplicate assets.

Same JSON format as the iOS app. Generated by the Python pipeline in the iOS repo (`scripts/shared/build_database.py` + `dedup_text.py`), and these files are byte-for-byte copies of `OrthodoxCalendar/Localization/` there — copy the whole set across rather than regenerating separately, or the two apps drift. Downloaded v2-archive years reference the SAME bundled pools, so a pool that is missing entries makes far-year texts render empty.

## Key Design Patterns

### Dark Mode
`AppColors` uses `LocalIsDarkTheme.current` (not `isSystemInDarkTheme()`) so it respects the app's theme setting (SYSTEM/LIGHT/DARK). The `OrthodoxCalendarTheme` provides `LocalIsDarkTheme` via `CompositionLocalProvider`.

### Localization
No Android string resources — all UI text comes from bundled JSON files loaded by `LocalizationManager`. Language switching reloads the bundle and triggers recomposition via `CalendarUiState.localization`.

### ViewModel State
Single `CalendarUiState` data class exposed via `StateFlow`. Contains: currentMonth, currentYear, daysInMonth, viewMode (LIST/GRID), language, theme, localization bundle, scrollToTodayTrigger.

### CalendarDay Model
- `dayOfWeek`: Python convention (0=Mon, 6=Sun), NOT Android convention
- `weekdayIndex`: converts to localization array index (0=Sun)
- `Feast.moveable`: distinguishes algorithmic feasts from fixed saints
- `Feast.description`: expandable text for major feasts

### Saint Bio Matching (engine/BioMatcher.kt)
Port of iOS `BioMatcher.swift`, assigning the whole day at once — which feast a bio belongs to depends on what the other feasts claim, so `SaintsSection` calls `BioMatcher.assign` once and looks each card up in the result.
- Rank, place and feast words (`BioMatcher.generic`, one list for all locales) are dropped before comparing; two-letter words count only when capitalised (Ор, II).
- Words match past inflection, spelling variants and typos (Вартоломеј/Вартоломеја, Тедот/Теодот) via shared prefixes and edit distance; a single substitution inside a short name (Матија/Марија) does not.
- A bio whose title *is* a feast's name is paired with that feast first and never lands anywhere else; the rest go best-first, and a weak match is taken only when it is the sole remaining candidate. A wrong bio is worse than none.
- Moveable feasts never get bios. `Feast.description` takes priority over bio text.
- `scripts/shared/simulate_bio_matching.py` in the iOS repo is the reference implementation for all three ports. `BioMatcherTest` checks this one against `app/src/test/resources/bio_assignments_2026.tsv`, generated from it over the bundled calendars (`python3 scripts/shared/simulate_bio_matching.py --tsv=<this repo>/app/src/test/resources/bio_assignments_2026.tsv`, run in the iOS repo after regenerating its `data/output`) — regenerate the fixture when the rules or the bundled data change, never edit it to make a failure pass.

### Haptic Feedback
`Haptics` utility uses `View.performHapticFeedback()` with sharp single-tick constants for responsiveness:
- `light` (`KEYBOARD_TAP`) — Search, Settings, Share, date picker
- `medium` (`VIRTUAL_KEY`) — Today button
- `selection` (`CONTEXT_CLICK`) — Month arrows, grid/list toggle, grid day tap

### Navigation
Compose Navigation with `NavGraph`. Routes: Splash → Calendar → DayDetail, Calendar → Search/Settings/About. DatePicker shown as `ModalBottomSheet` from CalendarTabScreen.

### Share & Reminders
- Share: `Intent.ACTION_SEND` with plain text
- Reminders: `Intent.ACTION_INSERT` to `CalendarContract.Events.CONTENT_URI`

## Locales

Four locales: `SR`, `RU`, `EN`, `EN_NC` (New Calendar).

`EN_NC` shares `en.json` but loads `calendar_en_nc_*.json` data. Fixed feasts on Gregorian dates (Dec 25 = Christmas). All `when` expressions use `AppLanguage.EN, AppLanguage.EN_NC ->` for English text.

## Common Pitfalls

1. **AppColors composable getters**: All adaptive colors are `@Composable get()` properties. Cannot be called from non-composable functions — pass colors as parameters or make the function `@Composable`.
2. **JSON in assets, not resources**: Calendar files go in `assets/localization/`, not `res/raw/`. Loaded via `context.assets.open()`.
3. **CalendarDay identity**: Use `gregorianDate` as key in `LazyColumn`. When locale changes, recompose by changing the list key.
4. **MonthHeaderBar callbacks**: `onPreviousMonth`/`onNextMonth` are `() -> Unit`. The IconButton `onClick` wraps them with haptics — don't double-wrap.
5. **Fasting colors**: `fastStrictBg`, `fastWaterBg`, etc. are `@Composable` getters. Use them inside composable scope only.

## Review findings

`docs/code-review-pass-3.md` holds the findings of the third full-codebase
review (P1 stale month under a new month header, P1 blank app on a failed DataStore
read, P2 permanent spinner when a day cannot be resolved, P2 unguarded share intent,
P2 missing fasting badge on Jan 1 / Dec 31, plus P3 hygiene), annotated item by item: everything
in it is now fixed except the splash gating and the share-text `drop(1)`, both
deliberate and marked there, and its "Checked and found correct" list still stands. All
three passes are folded into `docs/release-notes-1.5.1.md` (pass three under
"Also fixed (third review pass)") — do not re-report anything from that file's
cleared list. `docs/handoff-pass-3.md` records the verified state and the rules below.

These are easy to undo by accident:

* **Never call `advanceUntilIdle()` in a test.** `CalendarViewModel.startTodayTicker`
  sleeps to midnight and re-arms, so the shared `TestCoroutineScheduler` never idles
  and `testDebugUnitTest` spins silently — indistinguishable from a hung compiler, and
  mistaken for one. `CalendarViewModelTest` advances a bounded slice instead and
  cancels the ViewModel's scope in a `finally`; write new tests through its
  `calendarTest` helper.
* **`TRIM_MEMORY_UI_HIDDEN` is not memory pressure** (`app/MemoryTrim.kt`);
  `MemoryTrimTest` fails if it ever becomes a releasing level. **`TRIM_MEMORY_BACKGROUND`
  is**, and must stay one: on API 34+ those two are the only levels an app is sent,
  so a release set without BACKGROUND never runs.
* **`CalendarViewModel` must keep `@JvmOverloads` on its constructor.** `viewModel()`
  builds it by reflection through `(Application)`, which Kotlin emits for a
  constructor with default arguments only when asked. Without it every launch
  crashes while every test that constructs the ViewModel directly still passes;
  `ViewModelFactoryTest` builds it the app's way.
* **"Today" is refreshed on ON_START, not only at midnight.** A coroutine `delay` on
  the main thread runs on uptime, which stops in deep sleep. `MainActivity` calls
  `refreshToday()` from a `LifecycleStartEffect`, which also re-arms the tick.

And the two consolidations a reviewer asked for: the archive bounds live in
`CalendarArchive` (declared in `ui/viewmodel/CalendarViewModel.kt`, with
`canGoPrevious`/`canGoNext` — nothing else may restate `2024`/`2099`), and the fasting
colour/label mapping in `ui/util/FastingVisuals.kt` — do not reintroduce per-screen
copies of either.
